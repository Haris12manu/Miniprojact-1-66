class Transactions {
  //class
  String title;
  String type;
  String gender;
  double amount;
  String textEditing;
  DateTime date;
  Transactions(
      {required this.title,
      required this.type,
      required this.gender,
      required this.amount,
      required this.textEditing,
      required this.date});
}
